<%= form_with(model: current_user.active_relationships.build, remote: true) do |f| %>
<div  id="follow-button"><%= hidden_field_tag :followed_id, @user.id %></div>
<%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>